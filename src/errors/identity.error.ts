export enum IdentityErrorCode {
  EmailIsNotExists = 1,
  OTPIsNotValid = 2,
  InvalidCredentials = 3,
  NoTokenProvider = 4,
  TokenInValid = 5,
  EmailNotActivated = 6,
  Invalid = 7,
  EmailIsExists = 8,
}
